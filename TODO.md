# TODO

## Run the project (local)

- [ ] Confirm how to execute commands in this environment (runner seems to reject `&&`).
- [ ] Install backend dependencies: `cd backend` then `npm install`.
- [ ] Ensure backend `.env` exists and is configured (MongoDB URI, PORT, JWT secret, CORS origin).
- [ ] Start backend: `npm run dev` (or `npm start`).
- [ ] Install frontend dependencies: `cd frontend` then `npm install`.
- [ ] Ensure frontend `.env` exists and is configured (REACT_APP_API_URL, REACT_APP_SOCKET_URL).
- [ ] Start frontend: `npm start`.
- [ ] Verify backend health endpoint: `http://localhost:5000/api/health`.
- [ ] Verify frontend: `http://localhost:3000`.

